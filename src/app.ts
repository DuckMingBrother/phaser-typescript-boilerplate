import 'p2';
import 'pixi';
import 'phaser';

import { Game, AUTO } from 'phaser-ce';

window.onload = () => {
    const game = new Game(800, 600, AUTO, null, {
        preload() {

        },
        create() {

        },
        update() {

        }
    });
};
